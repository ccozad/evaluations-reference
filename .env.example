# Copy to .env for local runs. The `evals` CLI loads .env automatically.
# Required for the model-as-judge lens (Lens 2).
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: set to 1 to auto-skip the interactive human-grade lens (Lens 3).
# SKIP_HUMAN_EVAL=1
